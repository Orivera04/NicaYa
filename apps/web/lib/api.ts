const base=process.env.NEXT_PUBLIC_API_URL||"http://localhost:4000/api"; export type Session={accessToken:string;refreshToken:string;user:{id:string;name:string;email:string;role:"CLIENT"|"RIDER"|"ADMIN"}}; export const getSession=()=>typeof window==="undefined"?null:JSON.parse(localStorage.getItem("motoya-session")||"null") as Session|null; export const setSession=(s:Session)=>localStorage.setItem("motoya-session",JSON.stringify(s)); export const logout=()=>localStorage.removeItem("motoya-session"); export async function api<T>(path:string,options:RequestInit={}){const s=getSession();const r=await fetch(base+path,{...options,headers:{"Content-Type":"application/json",...(s?{Authorization:`Bearer ${s.accessToken}`}:{}) ,...options.headers}});if(!r.ok){const x=await r.json().catch(()=>null);throw new Error(x?.error?.message||"No fue posible completar la acción.");}return r.status===204?null as T:r.json() as Promise<T>}
