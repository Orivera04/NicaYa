"use client";

import { useEffect, useState } from "react";
import { Guard } from "@/components/Guard";
import { MobileAppShell } from "@/components/MobileAppShell";
import { api, getSession } from "@/lib/api";
type Me = { clientProfile?: { activationStatus: string; phoneVerifiedAt?: string | null; identityVerifiedAt?: string | null } | null };
export default function ClientAccountPage() { const [me, setMe] = useState<Me | null>(null); useEffect(() => { api<Me>("/auth/me").then(setMe).catch(() => undefined); }, []); const user = getSession()?.user; const status = me?.clientProfile?.activationStatus || "SEMI_ACTIVE"; const label: Record<string, string> = { REGISTERED: "Registro creado", SEMI_ACTIVE: "Cuenta semi activa", VERIFICATION_PENDING: "Verificacion en proceso", ACTIVE: "Cuenta verificada", REQUIRES_CORRECTION: "Informacion por corregir", SUSPENDED: "Cuenta suspendida", REJECTED: "Verificacion rechazada", DEACTIVATED: "Cuenta desactivada" }; return <Guard roles={["CLIENT"]}><MobileAppShell role="CLIENT"><h1 className="mt-4 text-2xl font-bold">Mi cuenta</h1><section className="mt-3 rounded-3xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-widest text-orange-400">Estado de activacion</p><b className="mt-1 block text-xl">{label[status]}</b><p className="mt-2 text-sm text-slate-300">{status === "ACTIVE" ? "Tu identidad y telefono estan verificados." : "Puedes explorar MotoYa y completar la verificacion interna cuando este disponible."}</p></section><section className="card mt-3"><b>{user?.name}</b><p className="muted">{user?.email}</p><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><p className="rounded-xl bg-slate-50 p-3">Telefono<br /><b>{me?.clientProfile?.phoneVerifiedAt ? "Verificado" : "Pendiente"}</b></p><p className="rounded-xl bg-slate-50 p-3">Identidad<br /><b>{me?.clientProfile?.identityVerifiedAt ? "Verificada" : "Pendiente"}</b></p></div></section></MobileAppShell></Guard>; }
