"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link"; import { api,setSession,Session } from "@/lib/api";
export default function Login(){const r=useRouter();const [error,setError]=useState("");const submit=async(f:FormData)=>{try{const s=await api<Session>("/auth/login",{method:"POST",body:JSON.stringify({email:f.get("email"),password:f.get("password")})});setSession(s);r.push(s.user.role==="ADMIN"?"/admin":s.user.role==="RIDER"?"/rider":"/client");}catch(e){setError((e as Error).message)}};return <main className="mx-auto max-w-md p-5"><Link href="/">← MotoYa</Link><h1 className="mt-10 text-3xl font-bold">Bienvenido</h1><form action={submit} className="mt-6 space-y-4"><input name="email" type="email" placeholder="Correo" required/><input name="password" type="password" placeholder="Contraseña" required/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="primary w-full">Entrar</button></form><p className="mt-4 text-center text-sm">¿No tienes cuenta? <Link className="text-orange-600" href="/register">Regístrate</Link></p></main>}
