"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { api,setSession,Session } from "@/lib/api";
export default function Register(){const r=useRouter();const [role,setRole]=useState("CLIENT");const [error,setError]=useState("");const submit=async(f:FormData)=>{try{const s=await api<Session>(`/auth/register/${role.toLowerCase()}`,{method:"POST",body:JSON.stringify({name:f.get("name"),email:f.get("email"),password:f.get("password"),phone:f.get("phone")})});setSession(s);r.push(role==="RIDER"?"/rider":"/client");}catch(e){setError((e as Error).message)}};return <main className="mx-auto max-w-md p-5"><h1 className="mt-8 text-3xl font-bold">Crear cuenta</h1><form action={submit} className="mt-6 space-y-4"><select value={role} onChange={e=>setRole(e.target.value)}><option value="CLIENT">Quiero solicitar viajes</option><option value="RIDER">Quiero ser rider</option></select><input name="name" placeholder="Nombre completo" required/><input name="phone" placeholder="Teléfono"/><input name="email" type="email" placeholder="Correo" required/><input name="password" type="password" minLength={8} placeholder="Contraseña (mínimo 8 caracteres)" required/>{error&&<p className="text-sm text-red-600">{error}</p>}<button className="primary w-full">Crear cuenta</button></form></main>}
