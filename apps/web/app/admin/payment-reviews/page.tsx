"use client";
import Link from "next/link";import {useEffect,useState}from"react";import{Guard}from"@/components/Guard";import{MobileAppShell}from"@/components/MobileAppShell";import{api}from"@/lib/api";
type Payment={id:string;status:string;amount:string;currency:string;externalReference:string;customerReference?:string|null;proofReference?:string|null;metadata?:{bankName?:string}|null;order:{planNameSnapshot:string;rider:{user:{name:string;email:string}}};method:{name:string}};
export default function PaymentReviewsPage(){const[payments,setPayments]=useState<Payment[]>([]);const[message,setMessage]=useState("");const load=()=>api<Payment[]>("/subscriptions/admin/payments").then(all=>setPayments(all.filter(item=>item.status==="PENDING_REVIEW"))).catch(error=>setMessage(error.message));useEffect(()=>{load()},[]);const review=async(id:string,approved:boolean)=>{const reason=approved?undefined:window.prompt("Motivo del rechazo")||undefined;if(!approved&&!reason)return;try{await api(`/subscriptions/admin/payments/${id}/review`,{method:"POST",body:JSON.stringify({approved,reason})});setMessage(approved?"Pago aprobado y suscripción activada.":"Pago rechazado.");load()}catch(error){setMessage((error as Error).message)}};return <Guard roles={["ADMIN"]}><MobileAppShell role="ADMIN"><section className="mt-4 flex justify-between gap-3"><div><p className="text-xs font-bold tracking-wider text-orange-500">SUSCRIPCIONES</p><h1 className="text-2xl font-black">Pagos por validar</h1><p className="muted">Comprueba el depósito antes de activar el plan.</p></div><Link className="text-sm font-bold text-orange-600" href="/admin/approvals">Expedientes</Link></section><section className="mt-4 space-y-3">{payments.map(payment=><article className="card" key={payment.id}><b>{payment.order.rider.user.name} · {payment.order.planNameSnapshot}</b><p className="muted">{payment.order.rider.user.email}</p><p className="mt-3 font-bold">{payment.method.name} · {payment.currency} {payment.amount}</p><p className="muted">Referencia MotoYa: {payment.externalReference}</p><p className="muted">Banco de origen: {payment.metadata?.bankName||"No indicado"}</p>{payment.customerReference?<p className="muted">Referencia del banco: {payment.customerReference}</p>:null}{payment.proofReference?.startsWith("data:image")?<img className="mt-3 max-h-72 w-full rounded-xl object-contain" src={payment.proofReference} alt="Comprobante de pago"/>:<p className="mt-3 text-sm text-red-600">No hay comprobante válido.</p>}<div className="mt-3 flex gap-2"><button className="primary flex-1" onClick={()=>review(payment.id,true)}>Aprobar pago</button><button className="border flex-1" onClick={()=>review(payment.id,false)}>Rechazar</button></div></article>)}{!payments.length?<p className="card">No hay comprobantes pendientes.</p>:null}</section>{message?<p className="mt-3 text-sm" role="status">{message}</p>:null}</MobileAppShell></Guard>}
