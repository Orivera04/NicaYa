"use client"; import { useEffect,useState } from "react"; import { Guard } from "@/components/Guard"; import { api } from "@/lib/api";
type Rider={id:string;name:string;email:string;riderProfile:{approval:string;subscriptions:{status:string;expiresAt:string}[]}};type Dashboard={clients:number;riders:number;pendingRiders:number;activeSubscriptions:number;requestedTrips:number;completedTrips:number;cancelledTrips:number;subscriptionRevenue:number};
export default function Admin(){const [dash,setDash]=useState<Dashboard|null>(null);const [riders,setRiders]=useState<Rider[]>([]);const [settings,setSettings]=useState<Record<string,string>>({});const [msg,setMsg]=useState("");const load=()=>Promise.all([api<Dashboard>("/admin/dashboard"),api<Rider[]>("/admin/riders"),api<Record<string,string>>("/admin/settings")]).then(([d,r,s])=>{setDash(d);setRiders(r);setSettings(s)}).catch(e=>setMsg(e.message));useEffect(()=>{load()},[]);const approve=async(id:string)=>{await api(`/admin/riders/${id}/status`,{method:"PATCH",body:JSON.stringify({approval:"APPROVED"})});load()};const sub=async(id:string)=>{await api(`/admin/riders/${id}/subscriptions`,{method:"POST",body:JSON.stringify({expiresAt:new Date(Date.now()+30*864e5).toISOString()})});load()};const save=async()=>{try{await api("/admin/settings",{method:"PATCH",body:JSON.stringify(settings)});setMsg("Configuración guardada.")}catch(e){setMsg((e as Error).message)}};return <Guard roles={["ADMIN"]}><main className="mx-auto max-w-5xl p-4"><header><p className="text-sm font-bold text-orange-500">MOTOYA ADMIN</p><h1 className="text-3xl font-bold">Operación</h1></header><section className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4">{dash&&Object.entries(dash).map(([k,v])=><div className="card" key={k}><p className="muted">{k}</p><b className="text-xl">{v}</b></div>)}</section><section className="card mt-5"><h2 className="font-bold">Tarifas configurables</h2><div className="mt-3 grid gap-2 md:grid-cols-3">{Object.entries(settings).map(([k,v])=><label key={k} className="text-sm">{k}<input value={v} onChange={e=>setSettings({...settings,[k]:e.target.value})}/></label>)}</div><button onClick={save} className="primary mt-3">Guardar cambios</button></section><section className="mt-5"><h2 className="font-bold">Riders</h2><div className="mt-2 space-y-2">{riders.map(r=><article className="card flex flex-col gap-2 md:flex-row md:items-center md:justify-between" key={r.id}><div><b>{r.name}</b><p className="muted">{r.email} · {r.riderProfile.approval} · {r.riderProfile.subscriptions[0]?.status||"Sin suscripción"}</p></div><div className="flex gap-2"><button className="border" onClick={()=>approve(r.id)}>Aprobar</button><button className="primary" onClick={()=>sub(r.id)}>Activar 30 días</button></div></article>)}</div></section>{msg&&<p className="mt-3 text-sm">{msg}</p>}</main></Guard>}
