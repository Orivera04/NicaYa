"use client"; import { useEffect,useState } from "react"; import { useRouter } from "next/navigation"; import { getSession,Session } from "@/lib/api"; export function Guard({roles,children}:{roles:Session["user"]["role"][];children:React.ReactNode}){const r=useRouter();const [s,setS]=useState<Session|null>();useEffect(()=>{const x=getSession();setS(x);if(!x||!roles.includes(x.user.role))r.replace("/login")},[r,roles]);if(!s)return <p className="p-6">Cargando…</p>;return <>{children}</>}
