import { PrismaClient } from "@prisma/client"; import bcrypt from "bcryptjs";
const prisma=new PrismaClient();
async function main(){const hash=await bcrypt.hash("password123",12);const admin=await prisma.user.upsert({where:{email:"admin@motoya.local"},update:{},create:{name:"Admin MotoYa",email:"admin@motoya.local",passwordHash:await bcrypt.hash("admin123",12),role:"ADMIN"}});const client=await prisma.user.upsert({where:{email:"client@motoya.local"},update:{},create:{name:"Cliente Demo",email:"client@motoya.local",passwordHash:hash,role:"CLIENT",clientProfile:{create:{}}}});const rider=await prisma.user.upsert({where:{email:"rider@motoya.local"},update:{},create:{name:"Rider Activo",email:"rider@motoya.local",passwordHash:hash,role:"RIDER",riderProfile:{create:{approval:"APPROVED",available:true,vehiclePlate:"MOTO-001",vehicleModel:"Honda CB"}}}});await prisma.user.upsert({where:{email:"pending-rider@motoya.local"},update:{},create:{name:"Rider Pendiente",email:"pending-rider@motoya.local",passwordHash:hash,role:"RIDER",riderProfile:{create:{approval:"PENDING"}}}});const profile=await prisma.riderProfile.findUniqueOrThrow({where:{userId:rider.id}});const sub=await prisma.riderSubscription.findFirst({where:{riderId:profile.id,status:"ACTIVE"}});if(!sub)await prisma.riderSubscription.create({data:{riderId:profile.id,status:"ACTIVE",startsAt:new Date(),expiresAt:new Date(Date.now()+30*864e5),pricePaid:10,currency:"USD",activatedById:admin.id}});for(const [key,value] of Object.entries({baseFare:"1.50",pricePerKm:"0.70",minimumFare:"2.00",currency:"USD",monthlySubscriptionPrice:"10.00"}))await prisma.systemSetting.upsert({where:{key},update:{value},create:{key,value}});console.log({admin:admin.email,client:client.email,rider:rider.email});} main().finally(()=>prisma.$disconnect());
