import crypto from "crypto";
import { z } from "zod";
import { availabilitySchema } from "@motoya/shared";
import { prisma } from "../db.js";
import { authenticate, authorize } from "../middleware/auth.js";
import { safeRouter } from "../middleware/safe-router.js";
import { getSettings } from "../services/settings.service.js";
import { assertRiderCanOperate } from "../services/subscription.service.js";
import { haversineKm } from "../lib/geo.js";
import { nicaraguaDayWindow } from "../lib/business-day.js";

export const ridersRouter = safeRouter();
ridersRouter.use(authenticate, authorize("RIDER"));

const profileSchema = z.object({ nationalId: z.string().trim().min(5).max(40), driverLicense: z.string().trim().min(5).max(40), vehiclePlate: z.string().trim().min(3).max(20), vehicleModel: z.string().trim().min(2).max(80), insuranceExpiresAt: z.coerce.date() });
const documentType = z.enum(["NATIONAL_ID", "DRIVER_LICENSE", "VEHICLE_REGISTRATION", "INSURANCE"]);
const requiredDocuments = ["NATIONAL_ID", "DRIVER_LICENSE", "VEHICLE_REGISTRATION", "INSURANCE"] as const;

async function activation(userId: string) {
  const now = new Date();
  const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId }, include: { documents: true, subscriptions: { where: { status: "ACTIVE", startsAt: { lte: now }, expiresAt: { gt: now } }, orderBy: { expiresAt: "desc" }, take: 1 } } });
  const profileComplete = Boolean(rider.nationalId && rider.driverLicense && rider.vehiclePlate && rider.vehicleModel && rider.insuranceExpiresAt);
  const docs = requiredDocuments.map((type) => rider.documents.find((document) => document.type === type));
  const insuranceValid = Boolean(rider.insuranceExpiresAt && rider.insuranceExpiresAt > now && docs.find((document) => document?.type === "INSURANCE")?.expiresAt && docs.find((document) => document?.type === "INSURANCE")!.expiresAt! > now);
  const allDocumentsApproved = docs.every((document) => document?.status === "APPROVED") && insuranceValid;
  const allDocumentsSubmitted = docs.every((document) => document && ["SUBMITTED", "UNDER_REVIEW", "APPROVED"].includes(document.status));
  const hasSubscription = rider.subscriptions.length > 0;
  let status = rider.onboardingStatus;
  if (!profileComplete) status = "PROFILE_INCOMPLETE";
  else if (rider.approval === "REJECTED") status = "REJECTED";
  else if (!allDocumentsSubmitted) status = "DOCUMENTS_PENDING";
  else if (rider.approval !== "APPROVED" || !allDocumentsApproved) status = "UNDER_REVIEW";
  else if (!hasSubscription) status = "SUBSCRIPTION_REQUIRED";
  else if (!rider.workZoneConfigured) status = "APPROVED";
  else status = "READY_TO_WORK";
  if (status !== rider.onboardingStatus) await prisma.riderProfile.update({ where: { id: rider.id }, data: { onboardingStatus: status } });
  const actions: Record<string, string> = { PROFILE_INCOMPLETE: "Completa tus datos de identidad y vehiculo.", DOCUMENTS_PENDING: "Envia los tres documentos requeridos.", UNDER_REVIEW: "Tus documentos estan en revision administrativa.", REQUIRES_CORRECTION: "Corrige los documentos indicados por el administrador.", REJECTED: "Tu solicitud fue rechazada. Contacta soporte para mas informacion.", SUBSCRIPTION_REQUIRED: "Activa una suscripcion para poder trabajar.", APPROVED: "Configura tu zona de trabajo para habilitarte.", READY_TO_WORK: "Ya puedes activar tu disponibilidad." };
  return { status, profileComplete, documents: rider.documents, documentsSubmittedAt: rider.documentsSubmittedAt, workZoneConfigured: rider.workZoneConfigured, workZoneDepartment: rider.workZoneDepartment, insuranceExpiresAt: rider.insuranceExpiresAt, insuranceValid, subscriptionActive: hasSubscription, nextAction: actions[status] || "Revisa el estado de tu cuenta." };
}

async function readiness(userId: string) {
  const now = new Date();
  const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId }, include: { user: true, documents: true, subscriptions: { where: { status: "ACTIVE", startsAt: { lte: now }, expiresAt: { gt: now } }, include: { plan: true }, take: 1 } } });
  const blockers: Array<{ code: string; message: string; action: string }> = [];
  const documentsValid = requiredDocuments.every((type) => rider.documents.find((document) => document.type === type)?.status === "APPROVED") && Boolean(rider.insuranceExpiresAt && rider.insuranceExpiresAt > now);
  if (rider.approval !== "APPROVED") blockers.push({ code: "APPROVAL_PENDING", message: "Tu expediente aún debe ser aprobado por administración.", action: "Ver mi cuenta" });
  if (rider.user.status !== "ACTIVE") blockers.push({ code: "ACCOUNT_INACTIVE", message: "Tu cuenta está suspendida o inactiva.", action: "Contactar soporte" });
  if (!documentsValid) blockers.push({ code: "DOCUMENTS_INVALID", message: "Debes tener los cuatro documentos aprobados y el seguro vigente.", action: "Revisar expediente" });
  if (!rider.subscriptions.length) blockers.push({ code: "SUBSCRIPTION_REQUIRED", message: "Necesitas una suscripción vigente para recibir solicitudes.", action: "Ver suscripción" });
  if (!rider.workZoneConfigured || rider.workZoneLat === null || rider.workZoneLng === null) blockers.push({ code: "WORK_ZONE_REQUIRED", message: "Configura tu zona de trabajo y comparte tu ubicación.", action: "Configurar zona" });
  const activeTrip = await prisma.trip.findFirst({ where: { riderId: userId, status: { in: ["ACCEPTED", "RIDER_ON_THE_WAY", "RIDER_ARRIVED", "IN_PROGRESS"] } }, select: { id: true, status: true } });
  if (activeTrip) blockers.push({ code: "ACTIVE_TRIP", message: "Tienes un viaje activo. Finalízalo antes de aceptar otro.", action: "Ver viaje" });
  const subscription = rider.subscriptions[0];
  const dailyTripLimit = subscription?.plan?.dailyTripLimit ?? null;
  const { start, end } = nicaraguaDayWindow(now);
  const completedToday = dailyTripLimit === null ? 0 : await prisma.trip.count({ where: { riderId: userId, status: "COMPLETED", updatedAt: { gte: start, lt: end } } });
  const remainingTrips = dailyTripLimit === null ? null : Math.max(0, dailyTripLimit - completedToday);
  if (remainingTrips === 0) blockers.push({ code: "DAILY_TRIP_LIMIT_REACHED", message: "Alcanzaste los viajes permitidos por tu plan para hoy.", action: "Vuelve mañana" });
  const subscriptionDaysRemaining = subscription ? Math.max(0, Math.ceil((subscription.expiresAt!.getTime() - now.getTime()) / 864e5)) : null;
  return { ready: blockers.length === 0, blockers, workZone: rider.workZoneConfigured ? { department: rider.workZoneDepartment, lat: rider.workZoneLat, lng: rider.workZoneLng, updatedAt: rider.workZoneUpdatedAt } : null, activeTrip, subscription: subscription ? { plan: subscription.plan?.name, expiresAt: subscription.expiresAt, daysRemaining: subscriptionDaysRemaining } : null, dailyQuota: dailyTripLimit === null ? null : { limit: dailyTripLimit, completed: completedToday, remaining: remainingTrips, resetsAt: end } };
}

ridersRouter.get("/me", async (req, res) => res.json(await prisma.riderProfile.findUnique({ where: { userId: req.user!.id }, include: { documents: true, subscriptions: { orderBy: { createdAt: "desc" } } } })));
ridersRouter.get("/me/activation", async (req, res) => res.json(await activation(req.user!.id)));
ridersRouter.get("/me/readiness", async (req, res) => res.json(await readiness(req.user!.id)));
ridersRouter.patch("/me/onboarding/profile", async (req, res) => { const data = profileSchema.parse(req.body); await prisma.riderProfile.update({ where: { userId: req.user!.id }, data: { ...data, onboardingStatus: "DOCUMENTS_PENDING" } }); res.json(await activation(req.user!.id)); });
ridersRouter.post("/me/work-zone/detect", async (req, res) => { const data = z.object({ lat: z.number().min(-90).max(90), lng: z.number().min(-180).max(180) }).parse(req.body); let department = "Zona detectada"; try { const response = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${data.lat}&lon=${data.lng}&format=jsonv2&zoom=10`, { headers: { "User-Agent": "MotoYa-MVP/1.0" } }); const place = await response.json() as { address?: Record<string, string> }; department = place.address?.state || place.address?.county || place.address?.city || department; } catch {} res.json({ department, ...data }); });
ridersRouter.patch("/me/work-zone", async (req, res) => { const data = z.object({ department: z.string().trim().min(2).max(80), lat: z.number(), lng: z.number(), reason: z.enum(["INITIAL", "MANUAL_CHANGE", "OUTSIDE_ZONE"]).default("MANUAL_CHANGE") }).parse(req.body); const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId: req.user!.id } }); await prisma.$transaction(async (tx) => { await tx.riderProfile.update({ where: { id: rider.id }, data: { workZoneConfigured: true, workZoneDepartment: data.department, workZoneLat: data.lat, workZoneLng: data.lng, workZoneUpdatedAt: new Date() } }); await tx.workZoneHistory.create({ data: { riderId: rider.id, department: data.department, lat: data.lat, lng: data.lng, reason: data.reason } }); }); res.json(await activation(req.user!.id)); });
ridersRouter.get("/me/work-zone/history", async (req, res) => { const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId: req.user!.id } }); res.json(await prisma.workZoneHistory.findMany({ where: { riderId: rider.id }, orderBy: { createdAt: "desc" }, take: 10 })); });
ridersRouter.post("/me/documents/submit", async (req, res) => { const image = z.string().regex(/^data:image\/(jpeg|png|webp);base64,/).max(500000); const data = z.object({ documents: z.array(z.object({ type: documentType, frontImage: image, backImage: image })).length(4) }).parse(req.body); if (new Set(data.documents.map((document) => document.type)).size !== 4) return res.status(400).json({ error: { code: "DOCUMENT_SET_INVALID", message: "Debes enviar cedula, licencia, circulacion y seguro." } }); const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId: req.user!.id }, include: { documents: true } }); if (!rider.insuranceExpiresAt || rider.insuranceExpiresAt <= new Date()) return res.status(422).json({ error: { code: "INSURANCE_EXPIRED", message: "Registra una fecha de vencimiento de seguro vigente antes de enviar el expediente." } }); const completeCurrentFile = requiredDocuments.every((type) => rider.documents.find((document) => document.type === type)?.status === "APPROVED"); if (rider.documentsSubmittedAt && completeCurrentFile) return res.status(409).json({ error: { code: "DOCUMENTS_ALREADY_SUBMITTED", message: "Tu expediente ya fue validado." } }); await prisma.$transaction(async (tx) => { for (const document of data.documents) await tx.riderDocument.upsert({ where: { riderId_type: { riderId: rider.id, type: document.type } }, update: { frontImage: document.frontImage, backImage: document.backImage, expiresAt: document.type === "INSURANCE" ? rider.insuranceExpiresAt : null, status: "SUBMITTED", reviewedAt: null, reviewedById: null, rejectionReason: null }, create: { riderId: rider.id, type: document.type, frontImage: document.frontImage, backImage: document.backImage, expiresAt: document.type === "INSURANCE" ? rider.insuranceExpiresAt : null, status: "SUBMITTED" } }); await tx.riderProfile.update({ where: { id: rider.id }, data: { documentsSubmittedAt: new Date(), onboardingStatus: "UNDER_REVIEW" } }); }); res.status(201).json(await activation(req.user!.id)); });
ridersRouter.patch("/me/availability", async (req, res) => { const data = availabilitySchema.parse(req.body); if (data.available) { await activation(req.user!.id); const state = await readiness(req.user!.id); if (!state.ready) return res.status(403).json({ error: { code: "RIDER_NOT_READY", message: state.blockers[0]?.message || "No puedes conectarte todavía.", details: state.blockers } }); await assertRiderCanOperate(req.user!.id); } const rider = await prisma.riderProfile.update({ where: { userId: req.user!.id }, data: { available: data.available } }); await prisma.auditLog.create({ data: { actorId: req.user!.id, action: data.available ? "RIDER_CONNECTED" : "RIDER_DISCONNECTED", entity: "RiderProfile", entityId: rider.id } }); req.app.get("io")?.to("admins").emit("rider:availability-updated", rider); res.json(rider); });
ridersRouter.patch("/me/location", async (req, res) => { const data = z.object({ lat: z.number().min(-90).max(90), lng: z.number().min(-180).max(180) }).parse(req.body); const rider = await prisma.riderProfile.update({ where: { userId: req.user!.id }, data: { workZoneLat: data.lat, workZoneLng: data.lng, workZoneUpdatedAt: new Date() } }); req.app.get("io")?.to("admins").emit("rider:location-updated", { riderId: rider.id, lat: data.lat, lng: data.lng }); res.json({ lat: data.lat, lng: data.lng, updatedAt: rider.workZoneUpdatedAt }); });
ridersRouter.get("/me/subscription", async (req, res) => { const now = new Date(); const current = await prisma.riderSubscription.findFirst({ where: { rider: { userId: req.user!.id }, status: "ACTIVE", startsAt: { lte: now }, expiresAt: { gt: now } }, include: { plan: true, payment: true }, orderBy: { expiresAt: "desc" } }); res.json(current ?? await prisma.riderSubscription.findFirst({ where: { rider: { userId: req.user!.id } }, include: { plan: true, payment: true }, orderBy: { createdAt: "desc" } })); });
ridersRouter.get("/me/subscription/payments", async (req, res) => res.json(await prisma.subscriptionPayment.findMany({ where: { rider: { userId: req.user!.id } }, orderBy: { createdAt: "desc" }, take: 10 })));
ridersRouter.post("/me/subscription/payments", async (req, res) => { const data = z.object({ amount: z.number().positive().optional() }).parse(req.body); const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId: req.user!.id } }); const settings = await getSettings(); const amount = data.amount ?? Number(settings.monthlySubscriptionPrice); if (amount < Number(settings.monthlySubscriptionPrice)) return res.status(422).json({ error: { code: "INVALID_PAYMENT_AMOUNT", message: `El monto minimo es ${settings.monthlySubscriptionPrice} ${settings.currency}.` } }); const payment = await prisma.subscriptionPayment.create({ data: { riderId: rider.id, reference: String(crypto.randomInt(100000, 1000000)), amount, currency: settings.currency, expiresAt: new Date(Date.now() + 60 * 60_000) } }); res.status(201).json(payment); });
ridersRouter.get("/available-trips", async (req, res) => { await assertRiderCanOperate(req.user!.id); const rider = await prisma.riderProfile.findUniqueOrThrow({ where: { userId: req.user!.id } }); if (rider.workZoneLat === null || rider.workZoneLng === null) return res.json([]); const settings = await getSettings(); const radiusKm = Math.min(50, Math.max(1, Number(settings.riderMatchRadiusKm) || 8)); const trips = await prisma.trip.findMany({ where: { status: "REQUESTED", expiresAt: { gt: new Date() } }, orderBy: { createdAt: "desc" }, take: 50 }); res.json(trips.filter((trip) => haversineKm({ lat: rider.workZoneLat!, lng: rider.workZoneLng! }, { lat: trip.originLat, lng: trip.originLng }) <= radiusKm).map((trip) => ({ ...trip, riderDistanceKm: haversineKm({ lat: rider.workZoneLat!, lng: rider.workZoneLng! }, { lat: trip.originLat, lng: trip.originLng }), matchRadiusKm: radiusKm }))); });
